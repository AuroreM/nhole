CREATE TABLE client (
    firstname varchar(40),
    lastname varchar(40),
    number varchar(40),
    morning boolean,
    lunch boolean,
    afternoon boolean,
    evening boolean,
    id bigserial
);
